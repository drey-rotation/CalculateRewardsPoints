const FetchTransactions = require('../FetchTransactions'); 
// const CalculatePoints = require('./CalculatePoints');


describe('Test the Rewards calculations', () => {

  it('Should calculate the results from a simple transaction over $100', async () => {
  });

  it('Should calculate the results from simple transaction between $50 and $100', async () => {
  });

  it('Should calculate the results from simple transaction less than $50 and greater than zero', async () => {
  });

  

})