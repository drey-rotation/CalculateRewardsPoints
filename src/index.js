import React from 'react';
import ReactDOM from 'react-dom/client';
import RewardsPointsApp from './app';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RewardsPointsApp />
  </React.StrictMode>
);


